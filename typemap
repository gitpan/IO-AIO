aio_req		T_AIO
aio_req_ornot	T_AIO_ORNOT

INPUT

T_AIO
	if (!($var = SvAIO_REQ ($arg)))
          croak (\"busy \" AIO_REQ_KLASS \" object expected\");

T_AIO_ORNOT
	if (!($var = SvAIO_REQ ($arg)))
	  XSRETURN_EMPTY;

